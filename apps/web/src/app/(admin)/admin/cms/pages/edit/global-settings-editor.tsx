import type { FC } from 'react';

interface Props {
  content: string;
  onChange: (value: string) => void;
}

export const GlobalSettingsEditor: FC<Props> = ({ content, onChange }) => {
  return null;
};
